# Mybio — Fresh Start

This repository was reset and initialized with a minimal starter project.

Quick start:

```bash
# serve locally
python3 -m http.server 8000

# then open http://localhost:8000
```
